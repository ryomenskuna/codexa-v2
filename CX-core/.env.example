PORT=4000

# Database configuration
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=codexa

# Auth / security
JWT_SECRET=change_this_to_a_long_random_secret
BCRYPT_SALT_ROUNDS=10

# CORS
FRONTEND_ORIGIN=http://localhost:5173
# Optional secondary origin (e.g. production URL)
# FRONTEND_ORIGIN_SECONDARY=https://codexa.your-domain.com
# Set to \"true\" only if you actually use cookies with credentials
CORS_CREDENTIALS=false

